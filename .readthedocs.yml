version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    post_install:
      - $READTHEDOCS_VIRTUALENV_PATH/bin/python -m pip install "sphinx-rtd-theme>=3.0.0"

conda:
  environment: environment.yml

sphinx:
  configuration: docs/source/conf.py