version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    post_install:
      - python -m pip install sphinx-rtd-theme

conda:
  environment: environment.yml

sphinx:
  configuration: docs/source/conf.py